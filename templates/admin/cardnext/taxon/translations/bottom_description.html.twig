{% set form = hookable_metadata.context.form %}
{% set locale = hookable_metadata.context.locale %}

{% if form.bottomDescription is defined %}
    <div class="col-12">
        {{ form_row(
            form.bottomDescription,
            sylius_test_form_attribute('bottom-description', locale)
        ) }}
    </div>
{% endif %}
