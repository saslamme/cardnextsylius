{% set form = hookable_metadata.context.form %}
{% set type = hookable_metadata.configuration.type %}
{% set field = hookable_metadata.configuration.field %}

<div class="cn-checkout-different-address">
    {{ form_row(
        attribute(form, field),
        sylius_test_form_attribute('different-' ~ type ~ '-address')
    ) }}
</div>
