<div class="cn-breadcrumbs"><div class="cn-container cn-breadcrumbs__inner">
<a href="{{ path('sylius_shop_homepage', {}) }}">{{ 'cardnext.storefront.common.home'|trans }}</a><span class="cn-breadcrumbs__sep" aria-hidden="true">/</span>
{% if taxon.parent %}<a href="{{ path('sylius_shop_product_index', {'slug': taxon.parent.slug}) }}">{{ taxon.parent.name }}</a><span class="cn-breadcrumbs__sep" aria-hidden="true">/</span>{% endif %}
<span aria-current="page">{{ taxon.name }}</span>
</div></div>
